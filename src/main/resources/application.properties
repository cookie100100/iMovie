spring.application.name=iMovie

# Active Profile
spring.profiles.active=dev

# Shared JPA Settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=iMovieSecretKeyForJWTAuthenticationPleaseChangeInProduction2026
jwt.expiration=86400000
